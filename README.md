# ✅ Task Manager – React

A clean and modern task manager application built using React.  
Users can add tasks, mark as completed by clicking on them, and delete tasks.  
Tasks are saved in the browser using `localStorage` — so they remain even after refreshing the page.

---

## ✨ Features

- ➕ Add new tasks
- ✔️ Click to mark tasks as completed
- ❌ Delete tasks
- 🔄 Persist tasks using `localStorage`
- 🎨 Professional UI with animations (Glassmorphism style)
- 📱 Fully responsive layout

---

## 🛠️ Tech Stack

| Technology | Purpose |
|-----------|---------|
| React | Frontend framework |
| JavaScript (ES6+) | Logic & interactivity |
| CSS3 | Custom styling & animations |

---

## 🚀 Getting Started

```bash
# Clone the repository
git clone https://github.com/Damadel/task-manager-react-project.git

cd task-manager-react-project

# Install dependencies
npm install

# Run development server
npm start
